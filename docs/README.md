# Burrow docs

- [Setup](./setup.md) — sandbox checklist for SSH, `gh`, and `glab` so the
  built-in implementation intents (`CodeWrite`, `FixPr`, `IntentCreate`) can
  push branches and open pull/merge requests.
- [Configuration](./configuration.md) — every field accepted by `.burrow/config.ts`,
  including agent, sandbox, SSH forwarding, network, and Git workflow options.
